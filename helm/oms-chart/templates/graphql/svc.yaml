apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: graphql{{ .Values.env_hyp }}
  labels:
    app: api
spec:
  selector:
    app: api
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
