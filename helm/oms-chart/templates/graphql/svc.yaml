apiVersion: v1
kind: Service
metadata:
  name: app
  namespace: graphql{{ .Values.env_hyp }}
  labels:
    app: app
spec:
  selector:
    app: app
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
