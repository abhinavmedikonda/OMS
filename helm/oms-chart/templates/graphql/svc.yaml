apiVersion: v1
kind: Service
metadata:
  name: graphql
  labels:
    app: graphql
spec:
  selector:
    app: graphql
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
