apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: graphql{{ .Values.env_hyp }}
  # annotations:
  #   nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
  - host: "oms.local"
    http:
      paths:
      - pathType: Prefix
        path: "/graphql"
        backend:
          service:
            name: graphql{{ .Values.env_hyp }}
            port:
              number: 80
      - pathType: Prefix
        path: "/playground"
        backend:
          service:
            name: graphql{{ .Values.env_hyp }}
            port:
              number: 80
